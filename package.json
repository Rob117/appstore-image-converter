{
  "name": "appstore-image-converter",
  "version": "0.0.1",
  "description": "Convert images into appstore-compatible image files",
  "main": "index.js",
  "repository": "git@github.com:Rob117/appstore-image-converter.git",
  "author": "Rob + Risa",
  "license": "MIT",
  "scripts": {
    "build": "tsc --lib es2017 index.ts",
    "start": "yarn build && node index.js"
  },
  "dependencies": {
    "imagemagick": "^0.1.3"
  },
  "devDependencies": {
    "@types/imagemagick": "^0.0.31",
    "typescript": "^5.5.3"
  }
}
